import type { ThreadShow } from "~/types/types-content"
import dbOp from "../db-op"
import time from "~/utils/basic/time"

export function setShowCountdown(
  oldThread: ThreadShow,
  memberId: string,
  userId: string,
) {
  
}
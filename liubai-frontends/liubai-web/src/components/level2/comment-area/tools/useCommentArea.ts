import type {
  CommentAreaProps,
  CommentAreaEmits,
} from "./types"

export function useCommentArea(
  props: CommentAreaProps,
  emit: CommentAreaEmits,
) {

  // 监听 comment store
  // 当有新的评论时，添加在最前面

  
}
// 获取当前分页的列表和详情两部分的宽度
// 注意：这不是全局的，比如 `首页` 和 `回收桶` 就会有各自的列表和详情的宽度

/**
 * 底部各层宽度:
 *   leftPx
 *   centerPx
 *   rightPx
 *     会依赖 useSidebarStore
 * 
 * 右边栏:
 *   detailPx
 *   minDetailPx
 *   maxDetailPx
 */

export const useWidthStore = () => {
  
}

